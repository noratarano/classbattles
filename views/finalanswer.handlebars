<!doctype html>
<html lang="en">

	<head>
		<title>ClassBattles</title>
		<meta charset="utf-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
		<link rel="shortcut icon" href="/images/favicon.ico">

		<!-- Loading Stylesheets -->
		<link href="/bootstrap/css/bootstrap.css" rel="stylesheet">
		<link href="/css/common.css" rel="stylesheet">
		<link href="/css/home.css" rel="stylesheet">
		<link href="/css/challenge.css" rel="stylesheet">

		<!-- HTML5 shim, for IE6-8 support of HTML5 elements. All other JS at the end of file. -->
		<!--[if lt IE 9]>
		  <script src="/js/lib/html5shiv.js"></script>
		  <script src="/js/lib/respond.min.js"></script>
		<![endif]-->
	</head>
	
	<body>
		<div class="container fill">
			
			<div class="row btn-group nav">
				<a href="#" class="btn btn-inverse nav-left invisible">
					<span class="fui-arrow-left"></span>
				</a>
				<div class="btn btn-inverse disabled nav-middle">
					<span>ClassBattles</span>
				</div>
				{{#if done}}
				<a href="/{{user.username}}/class/{{classname}}" class="btn btn-inverse nav-right">
					<span>{{classname}}</span>
					<span class="fui-arrow-right"></span>
				</a>
				{{else}}
				<a href="#" class="btn btn-inverse nav-right invisible">
					<span>next</span>
					<span class="fui-arrow-right"></span>
				</a>
				{{/if}}
			</div>
			
			<div class="row content">
				<h4>{{#if correct}}Correct!
					<span class="fui-check" style="color: #27AE60"></span>
					{{else}}
						{{#if timedOut}}
							Timed Out!
						{{else}}
							Wrong!
						{{/if}}
					<span class="fui-cross" style="color: #E74C3C"></span>
					{{/if}}
				</h4>
				{{#if correct}}
				<p>You responded:</p>
				<blockquote>
					<p>
						{{correctAnswer}}
						<small>Points will contribute toward:</small>
						{{#each tags}}
						<span class="btn btn-xs btn-info">{{tag}}</span>
						{{/each}}
					</p>
				</blockquote>
				{{else}}
					{{#if timedOut}}
					{{else}}
					<p>You responded:</p>
					<blockquote>
						<p>
							{{selected}}
						</p>
					</blockquote>
					{{/if}}
				<p>The correct answer was:</p>
				<blockquote>
					<p>
						{{correctAnswer}}
					</p>
				</blockquote>
				{{/if}}
              	<center>
					{{#if done}}
					<a class="btn btn-danger btn-block btn-lg btn-wide" href="/{{user.username}}/class/{{classname}}">
						You're done! Close.
					</a>
					{{else}}
					<a class="btn btn-primary btn-lg btn-wide" href="/{{user.username}}/class/{{classname}}/challenge/question">
						Next question
					</a>
					<a class="btn btn-danger btn-lg btn" href="/{{user.username}}/class/{{classname}}">
						Abandon
					</a>
					{{/if}}
				</center>
				<br/>
				<div class="race">
					<div class="race-me" style="margin-left:{{pixels userChallenger.currentChallenge.points}}px"><span class="fui-radio-checked"></span></div>
					{{#each challengers}}
					<div class="race-other" style="margin-left:{{pixels this.currentChallenge.points}}px"><span class="fui-radio-unchecked"></span></div>
					{{/each}}
				</div>
				<div class="race-pts">
					<div class="race-pts-left"><small>0%</small></div>
					<div class="race-pts-right"><small>100%</small></div>
				</div>
			</div>
		</div>
		<!-- /.container -->

		<!-- Load JS here for greater good =============================-->
		<script src="/js/lib/jquery-1.8.3.min.js"></script>
		<script src="/js/lib/jquery-ui-1.10.3.custom.min.js"></script>
		<script src="/js/lib/jquery.ui.touch-punch.min.js"></script>
		<script src="/js/lib/bootstrap.min.js"></script>
		<script src="/js/lib/bootstrap-select.js"></script>
		<script src="/js/lib/bootstrap-switch.js"></script>
		<script src="/js/lib/flatui-checkbox.js"></script>
		<script src="/js/lib/flatui-radio.js"></script>
		<script src="/js/lib/jquery.tagsinput.js"></script>
		<script src="/js/lib/jquery.placeholder.js"></script>
		
		<script src="/js/common.js"></script>
		<script src="/js/challenge.js"></script>
	</body>

</html>